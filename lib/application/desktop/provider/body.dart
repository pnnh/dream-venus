import 'package:flutter/material.dart';

class BodyProvider with ChangeNotifier {
  // double _offsetY = 0;
  //
  // int get flexPreview => 1000 - flexSource;
  //
  // double get offsetY => _offsetY;
  //
  // setOffsetY(double offsetY) {
  //   //_offsetY = offsetY;
  //   var delta = offsetY < 0 ? -(-offsetY).ceil() : offsetY.ceil();
  //   var newFlexSource = flexSource + delta;
  //   if (newFlexSource < 100)
  //     newFlexSource = 100;
  //   else if (newFlexSource > 900) newFlexSource = 900;
  //   flexSource = newFlexSource;
  //
  //   print("offsetY: $offsetY $delta $flexSource");
  //   notifyListeners();
  // }
}
